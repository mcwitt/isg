#include "state.h"
#include "params.h"
#include <stdio.h>

#define Z @Z@
#define NUM_BONDS @NUM_BONDS@

#define DEC_WARMUP 4
#define DEC_PER_MEAS @DEC_PER_MEAS@
#define UPDATES_PER_MEAS @UPDATES_PER_MEAS@

#define COL_WIDTH 14

#if (DEC_PER_MEAS > DEC_WARMUP)
#error Must have DEC_PER_MEAS <= DEC_WARMUP
#endif

void run_sim(state_t *s,
             params_t *p,
             double T[NUM_REPLICAS],
             unsigned int seed,
             int dec_max,
             FILE *fp);
