srcdir = @srcdir@
VPATH = $(srcdir)

CC = mpicc
CFLAGS = -I. -I$(srcdir) -I/usr/include/gsl -Wall -O3 -fopenmp

LDLIBS = -lm -lgsl -lgslcblas

MODULES = mod_meas.o mod_hist.o mod_modes.o mod_spinstate.o

isg: sim.o output.o state.o exchange.o replica.o sample.o $(MODULES)


clean:
	$(RM) *.o
